meta {
  name: GET /ordens-servico/{osId}/orcamento
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/ordens-servico/{{osId}}/orcamento
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Escopo exigido: os:ler, orcamentos:ler

  Rode 00-auth/Login antes para preencher o token.
}

meta {
  name: POST /ordens-servico/{osId}/pecas
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/ordens-servico/{{osId}}/pecas
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "itens": [
      {
        "itemId": "{{clienteId}}",
        "quantidade": 10.0
      }
    ]
  }
}

docs {
  Escopo exigido: os:escrever

  Rode 00-auth/Login antes para preencher o token.
}
